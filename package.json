{
  "name": "hero-grid-channel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx roku-deploy",
    "deploy": "npx roku-deploy deploy",
    "remove": "npx roku-deploy delete",
    "dashboard": "node dashboard-server.js",
    "dashboard:dev": "set ROKU_IP=192.168.1.155 && set ROKU_PORT=8085 && set DASHBOARD_PORT=3000 && node dashboard-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2",
    "roku-deploy": "^3.12.1"
  }
}